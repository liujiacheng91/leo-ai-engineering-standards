# Test

## P0 Cases

- T1: isAllArChargesReleased 返回 false 时，processApCharges 调用 processArCharges 并返回
- T2: isAllArChargesReleased 返回 true 时，processApCharges 调用 executeApChargesProcessing
- T3: isAllArChargesReleased 默认返回 false（占位行为）
